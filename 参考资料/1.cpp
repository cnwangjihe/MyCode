struct Lump{
	Lump *pre,*nxt;
	int val[500];
	int cc;
}a[500],*begin,*end;
struct Lump *u[500];
int cnt;

u[i]=a+i;
cnt


Lump *now;
now=begin;

now=now->nxt;
now->val[10];

if(now->cc>2*k)
{
	Lump *nx=u[cnt];
	cnt--;
	//--now--nx--
	nx->nxt=now->nxt;
	now->nxt->pre=nx;
	now->nxt=nx;
	nx->pre=now;
}

if(now->cc<k/2)
{
	Lump *nx=now->nxt;
	//now--->nx
	nx->pre=now->pre;
	now->pre->nxt=nx;
	cnt++;
	u[cnt]=now;
	if(nxÌ«´ó)
		²ð¿ª 
}




struct Lump{
	int pre,nxt;
	int val[500];
	int cc;
}a[500]; 
int begin,*end;



int val[500][500],cc[500];
int pre[500],nxt[500],begin,end;







